workflows:
  ios-sideload-workflow:
    name: iOS Sideload Workflow (Stable Fix)
    instance_type: mac_mini_m2
    max_build_duration: 30
    environment:
      flutter: stable
    scripts:
      - name: Preparation
        script: |
          set -e
          rm -rf ios build
          flutter create --platforms=ios --org com.coolmag .
          flutter pub get
          
          # Умная правка Podfile: интегрируемся в существующий post_install
          # Мы заменяем стандартную заглушку на нашу логику установки версии 13.0
          cd ios
          sed -i '' "s/post_install do |installer|/post_install do |installer|\\
            installer.pods_project.targets.each do |target|\\
              target.build_configurations.each do |config|\\
                config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '13.0'\\
              end\\
            end/g" Podfile
          
          pod install
          cd ..

      - name: Build Unsigned Debug App
        script: |
          # Собираем DEBUG для iphoneos
          flutter build ios --debug --no-codesign

      - name: Package IPA
        script: |
          set -e
          mkdir -p build/ios/ipa/Payload
          cp -r build/ios/iphoneos/Runner.app build/ios/ipa/Payload/
          cd build/ios/ipa
          zip -r Runner.ipa Payload
          echo "IPA Created Successfully"
    artifacts:
      - build/ios/ipa/Runner.ipa
