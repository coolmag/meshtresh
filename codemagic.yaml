workflows:
  ios-sideload-workflow:
    name: iOS Sideload Workflow (Strict Codesign Bypass)
    instance_type: mac_mini_m2
    max_build_duration: 30
    environment:
      flutter: stable
    scripts:
      - name: Clean and Regenerate iOS Project
        script: |
          set -e
          rm -rf ios
          flutter create --platforms=ios --org com.coolmag .
          
          # English strings to avoid locale/encoding issues in build
          plutil -insert NSBluetoothAlwaysUsageDescription -string "App needs Bluetooth to connect with nearby devices in Mesh network." ios/Runner/Info.plist
          plutil -insert NSBluetoothPeripheralUsageDescription -string "App needs Bluetooth to discover other devices." ios/Runner/Info.plist
          plutil -insert NSLocalNetworkUsageDescription -string "Local network is used for direct message transfer." ios/Runner/Info.plist
          plutil -insert NSLocationWhenInUseUsageDescription -string "Location is required for Nearby Connections." ios/Runner/Info.plist
          plutil -insert NSBonjourServices -json '["_mp-connection._tcp", "_mp-connection._udp", "_crisis-mesh._tcp", "_crisis-mesh._udp"]' ios/Runner/Info.plist
          plutil -insert UIRequiresPersistentWiFi -bool true ios/Runner/Info.plist
          
          cd ios
          sed -i '' "s/post_install do |installer|/post_install do |installer|\\
            installer.pods_project.targets.each do |target|\\
              target.build_configurations.each do |config|\\
                config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '13.0'\\
              end\\
            end/g" Podfile

      - name: Install Dependencies
        script: |
          set -e
          flutter pub get
          cd ios
          pod install
          cd ..

      - name: Build Unsigned Release App
        script: |
          # 1. Сначала собираем Flutter (он создаст нужные скрипты и кеши, но может упасть на подписи)
          flutter build ios --release --no-codesign || echo "Flutter build partially finished, continuing to xcodebuild..."
          
          # 2. Жестко перехватываем сборку через нативный xcodebuild и принудительно отключаем ЛЮБЫЕ проверки подписей
          cd ios
          xcodebuild -workspace Runner.xcworkspace -scheme Runner -configuration Release -sdk iphoneos build CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY="" EXPANDED_CODE_SIGN_IDENTITY=""

      - name: Package IPA for Sideloadly
        script: |
          set -e
          mkdir -p build/ios/ipa/Payload
          
          # Ищем собранный Runner.app, так как xcodebuild иногда кладет его в другую директорию
          APP_DIR=$(find build -name "Runner.app" -type d | head -n 1)
          if [ -z "$APP_DIR" ]; then
             APP_DIR=$(find ios/build -name "Runner.app" -type d | head -n 1)
          fi
          
          echo "Found app at: $APP_DIR"
          cp -r "$APP_DIR" build/ios/ipa/Payload/
          
          cd build/ios/ipa
          zip -r Runner.ipa Payload
          echo "IPA Created Successfully"
    artifacts:
      - build/ios/ipa/Runner.ipa
